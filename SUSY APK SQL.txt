ALTER TABLE deudas ADD COLUMN tipo_pago INTEGER not NULL DEFAULT 1;
ALTER TABLE historial ADD COLUMN tipo_pago INTEGER not NULL DEFAULT 1;


CREATE TABLE IF NOT EXISTS metodoPago(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  tipo_pago VARCHAR(255) NOT NULL UNIQUE,
  abreviatura VARCHAR(2) NOT NULL UNIQUE
);


INSERT INTO metodoPago (id, abreviatura, tipo_pago) VALUES (1,'E','EFECTIVO'),
(2,'TB','TRANSFERENCIA BANCARIA'), (3,'G','GIROS');
